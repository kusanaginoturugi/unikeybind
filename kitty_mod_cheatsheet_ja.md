# kmod チートシート

参照元: `/home/onoue/.config/kitty/kitty.conf`

## 修飾キー

| 表記 | キー |
|---|---|
| `kmod` | `kitty_mod` = `Ctrl+Shift` |

このチートシートでは `kitty_mod` を `kmod` と省略します。
`kitty_mod ctrl+shift` は設定ファイルのコメント内にあるデフォルト値です。

## 有効な割り当て

`kitty.conf` で明示的に有効になっている `kmod` の割り当てです。

| ショートカット | kitty action | 説明 |
|---|---|---|
| `kmod+]` | `next_tab` | 次のタブへ移動 |
| `kmod+[` | `previous_tab` | 前のタブへ移動 |

## 設定ファイル内に記載されているデフォルト割り当て

以下は `kitty.conf` 内のコメント化されたデフォルト例に載っている割り当てです。
このファイルで明示的にカスタム設定されている行ではありません。
読みやすさのため、ショートカット表記では `kitty_mod` を `kmod` と書いています。

### クリップボードと選択範囲

| ショートカット | 動作 |
|---|---|
| `kmod+c` | クリップボードへコピー |
| `kmod+v` | クリップボードから貼り付け |
| `kmod+s` | 選択範囲から貼り付け |
| `kmod+o` | 選択範囲をプログラムへ渡す |

### スクロールとスクロールバック

| ショートカット | 動作 |
|---|---|
| `kmod+up` / `kmod+k` | 1 行上へスクロール |
| `kmod+down` / `kmod+j` | 1 行下へスクロール |
| `kmod+page_up` | 1 ページ上へスクロール |
| `kmod+page_down` | 1 ページ下へスクロール |
| `kmod+home` | 先頭へスクロール |
| `kmod+end` | 末尾へスクロール |
| `kmod+z` | 前のプロンプトへ移動 |
| `kmod+x` | 次のプロンプトへ移動 |
| `kmod+h` | スクロールバックを表示 |
| `kmod+g` | 直前のコマンド出力を表示 |
| `kmod+/` | スクロールバックを検索 |

### ウィンドウ

| ショートカット | 動作 |
|---|---|
| `kmod+enter` | 新しいウィンドウを開く |
| `kmod+n` | 新しい OS ウィンドウを開く |
| `kmod+w` | ウィンドウを閉じる |
| `kmod+]` | 次のウィンドウへ移動 |
| `kmod+[` | 前のウィンドウへ移動 |
| `kmod+f` | ウィンドウを前方へ移動 |
| `kmod+b` | ウィンドウを後方へ移動 |
| ``kmod+` `` | ウィンドウを先頭へ移動 |
| `kmod+r` | ウィンドウのリサイズを開始 |
| `kmod+1` ... `kmod+0` | 1 番目から 10 番目のウィンドウへ移動 |
| `kmod+f7` | 表示中のウィンドウを選んでフォーカス |
| `kmod+f8` | 表示中のウィンドウと入れ替え |

### タブ

| ショートカット | 動作 |
|---|---|
| `kmod+right` | 次のタブへ移動 |
| `kmod+left` | 前のタブへ移動 |
| `kmod+t` | 新しいタブを開く |
| `kmod+q` | タブを閉じる |
| `kmod+.` | タブを前方へ移動 |
| `kmod+,` | タブを後方へ移動 |
| `kmod+alt+t` | タブタイトルを設定 |

### レイアウトとフォントサイズ

| ショートカット | 動作 |
|---|---|
| `kmod+l` | 次のレイアウトへ切り替え |
| `kmod+equal` / `kmod+plus` / `kmod+kp_add` | フォントサイズを大きくする |
| `kmod+minus` / `kmod+kp_subtract` | フォントサイズを小さくする |
| `kmod+backspace` | フォントサイズをリセット |

### ヒントと kitten

| ショートカット | 動作 |
|---|---|
| `kmod+e` | ヒントで URL を開く |
| `kmod+p>f` | ファイルパスをヒント表示して貼り付けまたはプログラムで開く |
| `kmod+p>shift+f` | ファイルパスをヒント表示 |
| `kmod+p>c` | ファイルを選択 |
| `kmod+p>d` | ディレクトリを選択 |
| `kmod+p>l` | 行をヒント表示 |
| `kmod+p>w` | 単語をヒント表示 |
| `kmod+p>h` | ハッシュをヒント表示 |
| `kmod+p>n` | 行番号をヒント表示 |
| `kmod+p>y` | ハイパーリンクをヒント表示 |
| `kmod+u` | Unicode 入力 |

### kitty コマンドとデバッグ

| ショートカット | 動作 |
|---|---|
| `kmod+f1` | kitty ドキュメント概要を表示 |
| `kmod+f3` | コマンドパレットを開く |
| `kmod+f11` | フルスクリーンを切り替え |
| `kmod+f10` | 最大化を切り替え |
| `kmod+f2` | 設定ファイルを編集 |
| `kmod+escape` | kitty shell を開く |
| `kmod+delete` | ターミナルをクリアしてアクティブ画面をリセット |
| `kmod+f5` | 設定ファイルを再読み込み |
| `kmod+f6` | 設定をデバッグ |

### 背景の不透明度

| ショートカット | 動作 |
|---|---|
| `kmod+a>m` | 背景の不透明度を上げる |
| `kmod+a>l` | 背景の不透明度を下げる |
| `kmod+a>1` | 背景を不透明にする |
| `kmod+a>d` | 背景の不透明度をデフォルトに戻す |
